<ul class="page-subtitle nav">
  {#each account.subAccounts as sub}
    <li class="nav-item pe-2">
      <button type="button"
        class="btn {sub.subAccountCode === sub_account_code ? ' btn-secondary disabled' : 'btn-outline-secondary'}"
        on:click={() => {
          select(account.accountCode, sub.subAccountCode);
        }}>
        <BilingualText primary={sub.name} secondary={sub.nameVi} inline={true} />
      </button>
    </li>
  {/each}
</ul>
<script>
import {createEventDispatcher} from 'svelte';
import BilingualText from './bilingual-text.svelte';

export let account;
export let sub_account_code;

const dispatch = createEventDispatcher();

const select = (code, sub) => {
  dispatch('select', {
    code: code,
    sub: sub
  });
}
</script>
